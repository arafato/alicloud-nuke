# yaml-language-server: $schema=https://goreleaser.com/static/schema.json
version: 2

project_name: ali-nuke

before:
  hooks:
    - go mod tidy

builds:
  - id: ali-nuke
    binary: ali-nuke
    main: .
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64
    ldflags:
      - -s -w
      - -X github.com/arafato/ali-nuke/version.version={{.Version}}
      - -X github.com/arafato/ali-nuke/version.commit={{.ShortCommit}}
      - -X github.com/arafato/ali-nuke/version.date={{.Date}}

archives:
  - id: default
    format: tar.gz
    name_template: >-
      {{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}
    format_overrides:
      - goos: windows
        format: zip
    files:
      - README.md
      - LICENSE*
      - config-example.yaml

checksum:
  name_template: "checksums.txt"
  algorithm: sha256

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
      - "^ci:"
      - Merge pull request
      - Merge branch

release:
  github:
    owner: arafato
    name: alicloud-nuke
  draft: false
  prerelease: auto
  name_template: "{{.ProjectName}} v{{.Version}}"

brews:
  - name: ali-nuke
    repository:
      owner: arafato
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    directory: Formula
    homepage: "https://github.com/arafato/alicloud-nuke"
    description: "A command-line tool to delete all resources from an Alibaba Cloud account"
    license: "MIT"
    commit_author:
      name: goreleaserbot
      email: bot@goreleaser.com
    commit_msg_template: "Brew formula update for {{ .ProjectName }} version {{ .Tag }}"
    install: |
      bin.install "ali-nuke"
    test: |
      system "#{bin}/ali-nuke", "version"
